{
  "name": "digital-exhibition-voice-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env server.mjs",
    "build": "vite build",
    "start": "node --env-file-if-exists=.env server.mjs --production",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "express": "^5.1.0",
    "vite": "^7.1.0",
    "ws": "^8.18.3"
  }
}
